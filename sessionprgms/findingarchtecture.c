#include"stdio.h"
void main(){
	printf("program to find the architecture of the system \n");
	printf("the size of the architecture is %zu",8*sizeof(int*)); //depending upon the size of address byte the architecture is calculated
	}
